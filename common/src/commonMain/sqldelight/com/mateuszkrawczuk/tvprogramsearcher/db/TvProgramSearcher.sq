
CREATE TABLE Show(
id INTEGER NOT NULL PRIMARY KEY,
image TEXT NOT NULL,
name TEXT NOT NULL,
genres TEXT NOT NULL
);

insertItem:
INSERT OR REPLACE INTO Show(id,
                            image,
                            name,
                            genres
                            )VALUES(?,?,?,?);


selectAll:
SELECT * FROM Show;

deleteAll:
DELETE FROM Show;